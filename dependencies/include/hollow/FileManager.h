#pragma once

#include <hollow/Logger.h>

#include <sstream>
#include <fstream>
#include <string>
#include <iostream>

namespace hollow {
	namespace FileManager {
		std::string readShaderFile(std::string shaderPath);
		
	}
}

